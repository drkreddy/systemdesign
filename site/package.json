{
  "name": "labs-site",
  "private": true,
  "type": "module",
  "//": "Exists so node treats the labs' .js files as ES modules when running tests. Deliberately NOT inside site/blog, which is the Workers Build directory — a package.json there can trigger an npm install step on deploy.",
  "scripts": {
    "test": "node blog/test/ratelimit.test.js"
  }
}
